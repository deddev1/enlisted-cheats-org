---
import LanguageSwitcher from './LanguageSwitcher.astro';
import { getUi } from '../data/i18n';
import { getLocalizedPath, type PageId } from '../data/i18n/routing';
import { defaultLocale, type LocaleCode } from '../data/i18n/locales';
import { enlistedScreenshots, gameplayScreenshots } from '../data/enlisted';
import { productInfo, siteConfig } from '../data/site';
import { contentSrc, contentSrcSet, galleryTileSizes } from '../lib/responsive-images';

function shopImageAttrs(src: string) {
	const imageSrc = contentSrc(src);
	const srcset = contentSrcSet(src);
	return { src: imageSrc, ...(srcset ? { srcset } : {}) };
}

interface Props {
	locale?: LocaleCode;
	pageId?: PageId;
	overlay?: boolean;
}

const { locale = defaultLocale, pageId = 'home', overlay = false } = Astro.props;
const ui = getUi(locale);
const current = Astro.url.pathname;
const homeHref = getLocalizedPath('home', locale);
const cheatsHref = getLocalizedPath('hacks', locale);
const supportHref = getLocalizedPath('support', locale);
const shopLabel = 'Shop';
const pricingHref = getLocalizedPath('pricing', locale);
const updatesHref = getLocalizedPath('updates', locale);
const featuresHref = getLocalizedPath('features', locale);
const faqHref = getLocalizedPath('faq', locale);
const reviewsHref = '/reviews/';

const utilityLinks = [
	{ label: ui.nav.cheats ?? 'Enlisted Cheats', href: cheatsHref, pageId: 'hacks' as PageId },
	{ label: ui.nav.features, href: featuresHref, pageId: 'features' as PageId },
	{ label: ui.nav.pricing, href: pricingHref, pageId: 'pricing' as PageId },
];

const shopCards = [
	{
		title: `${productInfo.plans[0].label} — $${productInfo.plans[0].price}`,
		description:
			'ESP, aimbot, wallhack, and radar for 30 days. Anti-cheat updates while your license is active.',
		href: siteConfig.checkoutUrl,
		image: enlistedScreenshots.mainMenu.src,
		alt: enlistedScreenshots.mainMenu.alt,
	},
	{
		title: `${productInfo.plans[1].label} — $${productInfo.plans[1].price}`,
		description:
			'One-time purchase with no renewals. Full feature stack and anti-cheat updates for as long as you play.',
		href: siteConfig.checkoutUrl,
		image: enlistedScreenshots.espOverlay.src,
		alt: enlistedScreenshots.espOverlay.alt,
	},
] as const;

const mainLinksBefore = [
	{ label: ui.nav.home, href: homeHref, pageId: 'home' as PageId },
	{ label: ui.nav.cheats ?? 'Cheats', href: cheatsHref, pageId: 'hacks' as PageId },
];

const mainLinksAfter = [
	{ label: ui.nav.updates, href: updatesHref, pageId: 'updates' as PageId },
	{ label: ui.nav.pricing, href: pricingHref, pageId: 'pricing' as PageId },
	{ label: ui.nav.reviews ?? 'Reviews', href: reviewsHref, pageId: undefined },
	{ label: ui.nav.faq, href: faqHref, pageId: 'faq' as PageId },
];

const featureCards = [
	{
		label: ui.nav.features,
		tag: 'Overview',
		description: 'Full ESP, wallhack, aimbot, and radar stack in one Enlisted Cheats license for Windows PC.',
		href: featuresHref,
		pageId: 'features' as PageId,
		image: enlistedScreenshots.mainMenu.src,
		alt: enlistedScreenshots.mainMenu.alt,
	},
	{
		label: ui.nav.esp,
		tag: 'ESP',
		description: 'Red soldier silhouettes and distance tags on open ground so you spot pushes and flanks early.',
		href: getLocalizedPath('enlisted-esp', locale),
		pageId: 'enlisted-esp' as PageId,
		image: gameplayScreenshots.esp.src,
		alt: gameplayScreenshots.esp.alt,
	},
	{
		label: 'Wallhack',
		tag: 'Wallhack',
		description: 'Green wallhack boxes through wooden walls and trench fortifications before enemies peek.',
		href: getLocalizedPath('wallhack', locale),
		pageId: 'wallhack' as PageId,
		image: gameplayScreenshots.wallhack.src,
		alt: gameplayScreenshots.wallhack.alt,
	},
	{
		label: ui.nav.aimbot,
		tag: 'Aimbot',
		description: 'Scoped enemy highlights, soft aim FOV, and per-weapon profiles for rifles and SMGs.',
		href: getLocalizedPath('enlisted-aimbot', locale),
		pageId: 'enlisted-aimbot' as PageId,
		image: gameplayScreenshots.aimbot.src,
		alt: gameplayScreenshots.aimbot.alt,
	},
	{
		label: 'Radar',
		tag: 'Radar',
		description: '2D radar, skeleton ESP, snaplines, and soldier icons readable through smoke and hard cover.',
		href: getLocalizedPath('radar', locale),
		pageId: 'radar' as PageId,
		image: gameplayScreenshots.radarMap.src,
		alt: gameplayScreenshots.radarMap.alt,
	},
] as const;

function pathMatches(href: string, id?: PageId) {
	if (id === 'home') return current === href || current === `/${locale}/` || current === '/';
	if (id === 'hacks') {
		return current === href || current.startsWith(href) || current.startsWith('/enlisted-cheats');
	}
	if (href === '/reviews/') return current.startsWith('/reviews/');
	if (href === '/blog/') return current.startsWith('/blog/');
	if (href === '/guides/') return current.startsWith('/guides/');
	return current === href || current.startsWith(href);
}

function featureCardActive(cardPageId: PageId): boolean {
	if (pageId === cardPageId) return true;
	const card = featureCards.find((entry) => entry.pageId === cardPageId);
	if (!card) return false;
	return pathMatches(card.href, cardPageId);
}

const featuresActive = featureCards.some((card) => featureCardActive(card.pageId));
const activeFeatureCard =
	featureCards.find((card) => featureCardActive(card.pageId)) ?? featureCards[0];
---

<header class:list={['nav', { 'nav--overlay': overlay }]} data-nav>
	<div class="nav__backdrop" data-nav-backdrop hidden aria-hidden="true"></div>

	<div class="nav__shell">
		{
			!overlay && (
				<div class="nav__utility" aria-label="Utility">
					<a href={homeHref} class="nav__utility-logo" aria-label={siteConfig.name}>
						<img src={siteConfig.logoRaster} alt={siteConfig.name} width="28" height="28" decoding="async" />
					</a>
					<nav class="nav__utility-links" aria-label="Quick links">
						<div class="nav__utility-dropdown" data-nav-dropdown>
							<button type="button" class="nav__utility-link nav__utility-link--dropdown" aria-expanded="false">
								Cheats
								<svg class="nav__caret" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
									<path d="M7 10l5 5 5-5H7z" fill="currentColor"></path>
								</svg>
							</button>
							<div class="nav__utility-panel">
								{
									utilityLinks.map((link) => (
										<a
											href={link.href}
											class:list={[
												'nav__utility-panel-link',
												{ 'is-active': pathMatches(link.href, link.pageId) },
											]}
										>
											{link.label}
										</a>
									))
								}
							</div>
						</div>
						<a href={supportHref} class="nav__utility-link nav__utility-link--external">
							Support
							<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
								<path
									d="M14 3h7v7h-2V6.41l-9.29 9.3-1.42-1.42 9.3-9.29H14V3zM5 5h6V3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-6h-2v6H5V5z"
									fill="currentColor"></path>
							</svg>
						</a>
					</nav>
				</div>
			)
		}

		<div class="nav__main">
			<a href={homeHref} class="nav__brand" aria-label={siteConfig.name}>
				<svg class="nav__brand-icon" viewBox="0 0 64 64" aria-hidden="true" focusable="false">
					<path
						d="M32 6l8 12 14 2-10 10 2 14-14-8-14 8 2-14L10 20l14-2L32 6z"
						fill="rgba(255,255,255,0.92)"></path>
					<path d="M12 44h40" stroke="rgba(197,138,69,0.85)" stroke-width="3" stroke-linecap="round"></path>
					<path d="M18 38l14 10 14-10" stroke="rgba(197,138,69,0.65)" stroke-width="2.5" fill="none"></path>
				</svg>
				<span class="nav__brand-text">Enlisted</span>
			</a>

			<nav class="nav__menu" id="nav-panel" data-nav-panel aria-label="Primary">
				{
					mainLinksBefore.map((link) => (
						<a
							href={link.href}
							class:list={[
								'nav__menu-link',
								{ 'is-active': pathMatches(link.href, link.pageId) },
							]}
						>
							{link.label}
						</a>
					))
				}

				<div class="nav__features-wrap" data-nav-dropdown data-nav-features>
					<a
						href={featuresHref}
						class:list={['nav__menu-link', 'nav__features-trigger', { 'is-active': featuresActive }]}
						aria-haspopup="true"
						aria-expanded="false"
						data-nav-features-trigger
					>
						{ui.nav.features}
						<svg class="nav__caret" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
							<path d="M7 10l5 5 5-5H7z" fill="currentColor"></path>
						</svg>
					</a>
					<div class="nav__features-mega" role="region" aria-label={ui.nav.features} data-nav-features-panel>
						<div class="nav__features-head">
							<p class="nav__features-kicker">Enlisted cheat modules</p>
							<p class="nav__features-lead">
								{
									featureCardActive(activeFeatureCard.pageId)
										? activeFeatureCard.description
										: 'Browse ESP, wallhack, aimbot, and radar modules included in the Enlisted Cheats package.'
								}
							</p>
						</div>
						<div class="nav__features-scroll" tabindex="0" aria-label="Feature modules">
							{
								featureCards.map((card) => (
									<a
										href={card.href}
										class:list={['nav__features-card', { 'is-active': featureCardActive(card.pageId) }]}
										aria-current={featureCardActive(card.pageId) ? 'page' : undefined}
									>
										<div class="nav__features-card-media">
											<img
												{...shopImageAttrs(card.image)}
												sizes="(max-width: 960px) 80vw, 220px"
												alt={card.alt}
												width="640"
												height="360"
												loading="lazy"
												decoding="async"
											/>
											<span class="nav__features-card-tag">{card.tag}</span>
										</div>
										<div class="nav__features-card-body">
											<h3 class="nav__features-card-title">{card.label}</h3>
											<p class="nav__features-card-desc">{card.description}</p>
											{
												featureCardActive(card.pageId) ? (
													<span class="nav__features-card-current">Current page</span>
												) : null
											}
										</div>
									</a>
								))
							}
						</div>
					</div>
				</div>

				{
					mainLinksAfter.map((link) => (
						<a
							href={link.href}
							class:list={[
								'nav__menu-link',
								{ 'is-active': pathMatches(link.href, link.pageId) },
							]}
						>
							{link.label}
						</a>
					))
				}

				<div class="nav__shop-wrap" data-nav-shop data-nav-dropdown>
					<a
						href={siteConfig.checkoutUrl}
						class="nav__shop nav__shop-trigger"
						rel="noopener noreferrer"
						aria-haspopup="true"
						aria-expanded="false"
						data-nav-shop-trigger
					>
						<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
							<path d="M12 2l2.2 6.8H21l-5.5 4 2.1 6.7L12 15.8 6.4 19.5l2.1-6.7L3 8.8h6.8L12 2z" fill="currentColor"></path>
						</svg>
						{shopLabel}
						<svg class="nav__caret nav__shop-caret" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
							<path d="M7 10l5 5 5-5H7z" fill="currentColor"></path>
						</svg>
					</a>
					<div class="nav__shop-mega" role="region" aria-label={shopLabel} data-nav-shop-panel>
						<div class="nav__shop-mega-grid">
							{
								shopCards.map((card) => (
									<a href={card.href} class="nav__shop-card" rel="noopener noreferrer">
										<div class="nav__shop-card-media">
											<img
												{...shopImageAttrs(card.image)}
												sizes={galleryTileSizes}
												alt={card.alt}
												width="640"
												height="400"
												loading="lazy"
												decoding="async"
											/>
											<span class="nav__shop-card-icon" aria-hidden="true">
												<svg viewBox="0 0 24 24" focusable="false">
													<path
														d="M14 3h7v7h-2V6.41l-9.29 9.3-1.42-1.42 9.3-9.29H14V3zM5 5h6V3H5a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-6h-2v6H5V5z"
														fill="currentColor"></path>
												</svg>
											</span>
										</div>
										<div class="nav__shop-card-body">
											<h3 class="nav__shop-card-title">{card.title}</h3>
											<p class="nav__shop-card-desc">{card.description}</p>
										</div>
									</a>
								))
							}
						</div>
					</div>
				</div>

				<a href={siteConfig.checkoutUrl} class="nav__shop nav__shop--mobile" rel="noopener noreferrer">
					<svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
						<path d="M12 2l2.2 6.8H21l-5.5 4 2.1 6.7L12 15.8 6.4 19.5l2.1-6.7L3 8.8h6.8L12 2z" fill="currentColor"></path>
					</svg>
					{shopLabel}
				</a>
			</nav>

			<div class="nav__actions">
				<LanguageSwitcher pageId={pageId} currentLocale={locale} />
				<button
					type="button"
					class="nav__toggle"
					aria-label="Open menu"
					aria-expanded="false"
					aria-controls="nav-panel"
					data-nav-toggle
				>
					<span class="nav__toggle-icon" aria-hidden="true"></span>
				</button>
			</div>
		</div>
	</div>
</header>

<script>
	const nav = document.querySelector('[data-nav]');
	if (nav) {
		const toggle = nav.querySelector('[data-nav-toggle]');
		const panel = nav.querySelector('[data-nav-panel]');

		const setOpen = (open: boolean) => {
			toggle?.setAttribute('aria-expanded', String(open));
			toggle?.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
			nav.classList.toggle('is-open', open);
			document.body.classList.toggle('nav-open', open);
			const backdrop = nav.querySelector('[data-nav-backdrop]') as HTMLElement | null;
			if (backdrop) backdrop.hidden = !open;
		};

		toggle?.addEventListener('click', () => {
			setOpen(toggle.getAttribute('aria-expanded') !== 'true');
		});

		panel?.querySelectorAll('a').forEach((link) => {
			link.addEventListener('click', () => setOpen(false));
		});

		document.addEventListener('keydown', (event) => {
			if (event.key === 'Escape' && nav.classList.contains('is-open')) {
				setOpen(false);
			}
		});

		nav.addEventListener('click', (event) => {
			const target = event.target as Element;
			if (nav.classList.contains('is-open') && target.hasAttribute('data-nav-backdrop')) {
			 setOpen(false);
			}
		});

		const onScroll = () => nav.classList.toggle('is-scrolled', window.scrollY > 8);
		onScroll();
		window.addEventListener('scroll', onScroll, { passive: true });

		const closeAllDropdowns = (except?: Element | null) => {
			nav.querySelectorAll('[data-nav-dropdown]').forEach((dropdown) => {
				if (except && dropdown === except) return;
				dropdown.classList.remove('is-open');
				dropdown.querySelector('[aria-expanded]')?.setAttribute('aria-expanded', 'false');
			});
		};

		const bindDropdown = (dropdown: Element) => {
			const trigger = dropdown.querySelector(
				'button, [data-nav-shop-trigger], [data-nav-features-trigger]',
			) as HTMLElement | null;
			if (!trigger) return;

			let closeTimer: ReturnType<typeof setTimeout> | undefined;

			const setOpen = (open: boolean) => {
				clearTimeout(closeTimer);
				if (open) {
					closeAllDropdowns(dropdown);
					dropdown.classList.add('is-open');
					trigger.setAttribute('aria-expanded', 'true');
				} else {
					dropdown.classList.remove('is-open');
					trigger.setAttribute('aria-expanded', 'false');
				}
			};

			const scheduleClose = () => {
				clearTimeout(closeTimer);
				closeTimer = setTimeout(() => setOpen(false), 140);
			};

			if (window.matchMedia('(min-width: 961px)').matches) {
				dropdown.addEventListener('mouseenter', () => setOpen(true));
				dropdown.addEventListener('mouseleave', scheduleClose);
				dropdown.addEventListener('focusin', () => setOpen(true));
				dropdown.addEventListener('focusout', (event) => {
					if (!dropdown.contains((event as FocusEvent).relatedTarget as Node)) {
						scheduleClose();
					}
				});
			} else if (trigger.tagName === 'BUTTON') {
				trigger.addEventListener('click', () => {
					const isOpen = dropdown.classList.contains('is-open');
					closeAllDropdowns();
					setOpen(!isOpen);
				});
			} else if (trigger.hasAttribute('data-nav-features-trigger')) {
				trigger.addEventListener('click', (event) => {
					if (!window.matchMedia('(min-width: 961px)').matches) {
						event.preventDefault();
						const isOpen = dropdown.classList.contains('is-open');
						closeAllDropdowns();
						setOpen(!isOpen);
					}
				});
			}
		};

		const scrollActiveFeatureCard = (dropdown: Element) => {
			const activeCard = dropdown.querySelector('.nav__features-card.is-active') as HTMLElement | null;
			const scroller = dropdown.querySelector('.nav__features-scroll') as HTMLElement | null;
			if (!activeCard || !scroller) return;
			const offset =
				activeCard.offsetLeft - scroller.clientWidth / 2 + activeCard.clientWidth / 2;
			scroller.scrollTo({ left: Math.max(0, offset), behavior: 'smooth' });
		};

		nav.querySelectorAll('[data-nav-features]').forEach((dropdown) => {
			dropdown.addEventListener('mouseenter', () => scrollActiveFeatureCard(dropdown));
			dropdown.addEventListener('focusin', () => scrollActiveFeatureCard(dropdown));
		});

		nav.querySelectorAll('[data-nav-dropdown]').forEach(bindDropdown);
	}
</script>

<style is:global>
	body.nav-open {
		overflow: hidden;
	}

	.nav .lang-switcher__toggle {
		border-color: rgba(255, 255, 255, 0.14);
		background: rgba(0, 0, 0, 0.35);
		color: rgba(255, 255, 255, 0.88);
		border-radius: 0.2rem;
		font-size: 0.72rem;
		text-transform: uppercase;
		letter-spacing: 0.06em;
	}

	.nav .lang-switcher__panel {
		border-color: rgba(255, 255, 255, 0.12);
		background: rgba(18, 16, 14, 0.98);
	}
</style>

<style>
	.nav {
		position: sticky;
		top: 0;
		z-index: 100;
		pointer-events: none;
	}

	.nav--overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
	}

	.nav--overlay .nav__main {
		padding-top: calc(0.35rem + env(safe-area-inset-top, 0px));
		background: linear-gradient(
			180deg,
			rgba(13, 17, 18, 0.28) 0%,
			rgba(13, 17, 18, 0.12) 100%
		);
		border-bottom-color: rgba(255, 255, 255, 0.08);
	}

	.nav__shell {
		pointer-events: auto;
	}

	.nav__utility {
		display: flex;
		align-items: center;
		gap: 1rem;
		min-height: 2rem;
		padding: 0.35rem var(--page-gutter);
		padding-top: calc(0.35rem + env(safe-area-inset-top, 0px));
		background: rgba(13, 17, 18, 0.42);
		backdrop-filter: blur(12px);
		border-bottom: 1px solid rgba(255, 255, 255, 0.06);
	}

	.nav__utility-logo {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2rem;
		height: 2rem;
		border-radius: 0.15rem;
		background: rgba(255, 255, 255, 0.06);
	}

	.nav__utility-logo img {
		width: 1.35rem;
		height: 1.35rem;
		object-fit: contain;
	}

	.nav__utility-links {
		display: flex;
		align-items: center;
		gap: 1.25rem;
	}

	.nav__utility-link {
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
		color: rgba(255, 255, 255, 0.72);
		font-size: 0.68rem;
		font-weight: 700;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		text-decoration: none;
		transition: color 0.2s ease;
	}

	.nav__utility-link svg {
		width: 0.85rem;
		height: 0.85rem;
		opacity: 0.75;
	}

	.nav__utility-link:hover,
	.nav__utility-link.is-active {
		color: #fff;
	}

	.nav__utility-dropdown {
		position: relative;
	}

	.nav__utility-link--dropdown {
		border: 0;
		background: transparent;
		cursor: pointer;
		font: inherit;
	}

	.nav__utility-panel {
		position: absolute;
		top: calc(100% + 0.35rem);
		left: 0;
		z-index: 130;
		min-width: 11rem;
		padding: 0.35rem 0;
		border: 1px solid rgba(255, 255, 255, 0.1);
		background: rgba(12, 12, 12, 0.98);
		box-shadow: 0 14px 32px rgba(0, 0, 0, 0.45);
		opacity: 0;
		visibility: hidden;
		pointer-events: none;
		transform: translateY(-4px);
		transition:
			opacity 0.18s ease,
			transform 0.18s ease,
			visibility 0.18s ease;
	}

	.nav__utility-dropdown.is-open .nav__utility-panel,
	.nav__utility-dropdown:focus-within .nav__utility-panel {
		opacity: 1;
		visibility: visible;
		pointer-events: auto;
		transform: translateY(0);
	}

	.nav__utility-panel-link {
		display: block;
		padding: 0.55rem 1rem;
		color: rgba(255, 255, 255, 0.78);
		font-size: 0.68rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		text-decoration: none;
		transition:
			color 0.2s ease,
			background 0.2s ease;
	}

	.nav__utility-panel-link:hover,
	.nav__utility-panel-link.is-active {
		color: #fff;
		background: rgba(255, 255, 255, 0.06);
	}

	.nav__caret {
		width: 0.85rem;
		height: 0.85rem;
		opacity: 0.75;
		transition: transform 0.2s ease;
	}

	.nav__utility-dropdown.is-open .nav__caret,
	.nav__dropdown.is-open .nav__caret,
	.nav__features-wrap.is-open .nav__caret,
	.nav__shop-wrap.is-open .nav__shop-caret {
		transform: rotate(180deg);
	}

	.nav__main {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
		align-items: stretch;
		gap: 0;
		min-height: 3.5rem;
		padding: 0 var(--page-gutter);
		background: linear-gradient(
			180deg,
			rgba(13, 17, 18, 0.52) 0%,
			rgba(13, 17, 18, 0.34) 100%
		);
		border-bottom: 1px solid rgba(255, 255, 255, 0.08);
		backdrop-filter: blur(14px) saturate(1.1);
		-webkit-backdrop-filter: blur(14px) saturate(1.1);
		transition:
			background 0.25s ease,
			border-color 0.25s ease,
			backdrop-filter 0.25s ease;
	}

	.nav.is-scrolled .nav__main,
	.nav--overlay.is-scrolled .nav__main {
		background: linear-gradient(
			180deg,
			rgba(13, 17, 18, 0.78) 0%,
			rgba(13, 17, 18, 0.62) 100%
		);
		border-bottom-color: rgba(255, 255, 255, 0.12);
	}

	.nav--overlay.is-scrolled .nav__main {
		background: linear-gradient(
			180deg,
			rgba(13, 17, 18, 0.68) 0%,
			rgba(13, 17, 18, 0.5) 100%
		);
	}

	.nav__brand {
		display: inline-flex;
		align-items: center;
		gap: 0.55rem;
		color: #fff;
		text-decoration: none;
		min-width: 0;
		padding-right: 1.25rem;
		margin-right: 0.25rem;
		border-right: 1px solid rgba(255, 255, 255, 0.12);
		align-self: center;
		min-height: 3.5rem;
		grid-column: 1;
		justify-self: start;
	}

	.nav__brand-icon {
		width: 2rem;
		height: 2rem;
		flex-shrink: 0;
	}

	.nav__brand-text {
		font-family: var(--font-display);
		font-size: 1.35rem;
		font-weight: 800;
		letter-spacing: 0.14em;
		text-transform: uppercase;
		line-height: 1;
		white-space: nowrap;
	}

	.nav__menu {
		display: flex;
		align-items: stretch;
		justify-content: center;
		gap: 0;
		min-width: 0;
		grid-column: 2;
		justify-self: center;
	}

	.nav__menu-link,
	.nav__menu-link--dropdown {
		display: inline-flex;
		align-items: center;
		gap: 0.3rem;
		padding: 0 1rem;
		min-height: 3.5rem;
		color: rgba(255, 255, 255, 0.88);
		font-size: 0.84rem;
		font-weight: 600;
		letter-spacing: 0.01em;
		text-transform: none;
		text-decoration: none;
		border: 0;
		background: transparent;
		transition:
			color 0.2s ease,
			background 0.2s ease;
		white-space: nowrap;
	}

	.nav__menu > :first-child {
		border-left: 0;
	}

	.nav__menu-link--accent {
		color: rgba(255, 255, 255, 0.88);
	}

	.nav__menu-link--accent:hover,
	.nav__menu-link--accent.is-active {
		color: #fff;
	}

	.nav__menu-link:hover,
	.nav__menu-link.is-active,
	.nav__menu-link--dropdown:hover,
	.nav__menu-link--dropdown.is-active {
		color: #fff;
	}

	.nav__menu-link--dropdown {
		cursor: pointer;
		font: inherit;
	}

	.nav__dropdown {
		position: relative;
	}

	.nav__dropdown-panel {
		position: absolute;
		top: 100%;
		left: 0;
		z-index: 120;
		min-width: 12.5rem;
		padding: 0.45rem 0;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(24, 22, 20, 0.98);
		box-shadow: 0 16px 36px rgba(0, 0, 0, 0.45);
		opacity: 0;
		visibility: hidden;
		pointer-events: none;
		transform: translateY(-4px);
		transition:
			opacity 0.18s ease,
			transform 0.18s ease,
			visibility 0.18s ease;
	}

	.nav__dropdown.is-open .nav__dropdown-panel,
	.nav__dropdown:focus-within .nav__dropdown-panel {
		opacity: 1;
		visibility: visible;
		pointer-events: auto;
		transform: translateY(0);
	}

	.nav__dropdown-link {
		display: block;
		padding: 0.55rem 1.15rem;
		color: rgba(255, 255, 255, 0.82);
		font-size: 0.84rem;
		font-weight: 500;
		letter-spacing: 0;
		text-transform: none;
		text-decoration: none;
		transition:
			color 0.2s ease,
			background 0.2s ease;
	}

	.nav__dropdown-link:hover,
	.nav__dropdown-link.is-active {
		color: #fff;
		background: rgba(255, 255, 255, 0.06);
	}

	.nav__actions {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		flex-shrink: 0;
		align-self: center;
		padding-left: 0.75rem;
		grid-column: 3;
		justify-self: end;
	}

	.nav__features-wrap {
		position: relative;
		align-self: stretch;
	}

	.nav__features-trigger {
		height: 100%;
	}

	.nav__features-wrap.is-open .nav__features-trigger,
	.nav__features-wrap:focus-within .nav__features-trigger,
	.nav__features-trigger:hover {
		color: #fff;
		background: rgba(255, 255, 255, 0.04);
	}

	.nav__features-wrap.is-open .nav__features-trigger {
		box-shadow: inset 0 -2px 0 rgba(197, 138, 69, 0.85);
	}

	.nav__features-mega {
		position: fixed;
		top: calc(3.5rem + env(safe-area-inset-top, 0px));
		left: 50%;
		z-index: 145;
		width: min(52rem, calc(100vw - 2 * var(--page-gutter)));
		padding: 1rem 1rem 1.1rem;
		border: 1px solid rgba(197, 138, 69, 0.42);
		background: rgba(18, 16, 14, 0.98);
		box-shadow: 0 24px 56px rgba(0, 0, 0, 0.55);
		opacity: 0;
		visibility: hidden;
		pointer-events: none;
		transform: translateX(-50%) translateY(-6px);
		transition:
			opacity 0.2s ease,
			transform 0.2s ease,
			visibility 0.2s ease;
	}

	.nav:not(.nav--overlay) .nav__features-mega {
		top: calc(5.5rem + env(safe-area-inset-top, 0px));
	}

	.nav__features-wrap.is-open .nav__features-mega,
	.nav__features-wrap:focus-within .nav__features-mega {
		opacity: 1;
		visibility: visible;
		pointer-events: auto;
		transform: translateX(-50%) translateY(0);
	}

	.nav__features-head {
		display: grid;
		gap: 0.35rem;
		margin-bottom: 0.85rem;
		text-align: center;
	}

	.nav__features-kicker {
		margin: 0;
		color: var(--color-gold);
		font-size: 0.68rem;
		font-weight: 800;
		letter-spacing: 0.14em;
		text-transform: uppercase;
	}

	.nav__features-lead {
		margin: 0 auto;
		max-width: 36rem;
		color: rgba(255, 255, 255, 0.72);
		font-size: 0.84rem;
		line-height: 1.55;
	}

	.nav__features-scroll {
		display: flex;
		align-items: stretch;
		gap: 0.75rem;
		overflow-x: auto;
		padding-bottom: 0.15rem;
		scroll-snap-type: x mandatory;
		scrollbar-width: thin;
		scrollbar-color: rgba(197, 138, 69, 0.45) transparent;
	}

	.nav__features-scroll::-webkit-scrollbar {
		height: 0.35rem;
	}

	.nav__features-scroll::-webkit-scrollbar-thumb {
		border-radius: 999px;
		background: rgba(197, 138, 69, 0.45);
	}

	.nav__features-card {
		display: flex;
		flex: 0 0 min(14.5rem, 78vw);
		flex-direction: column;
		overflow: hidden;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(0, 0, 0, 0.35);
		color: inherit;
		text-decoration: none;
		scroll-snap-align: center;
		transition:
			border-color 0.2s ease,
			transform 0.2s ease,
			background 0.2s ease,
			box-shadow 0.2s ease;
	}

	.nav__features-card:hover {
		border-color: rgba(197, 138, 69, 0.45);
		background: rgba(197, 138, 69, 0.04);
		transform: translateY(-1px);
	}

	.nav__features-card.is-active {
		border-color: rgba(197, 138, 69, 0.75);
		background: rgba(197, 138, 69, 0.08);
		box-shadow: 0 0 0 1px rgba(197, 138, 69, 0.18);
	}

	.nav__features-card-media {
		position: relative;
		aspect-ratio: 16 / 10;
		overflow: hidden;
		background: rgba(255, 255, 255, 0.04);
	}

	.nav__features-card-media img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.nav__features-card-tag {
		position: absolute;
		top: 0.55rem;
		left: 0.55rem;
		padding: 0.18rem 0.45rem;
		background: rgba(88, 96, 75, 0.92);
		color: #f1eee5;
		font-size: 0.62rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.nav__features-card-body {
		display: grid;
		gap: 0.35rem;
		padding: 0.8rem 0.85rem 0.9rem;
	}

	.nav__features-card-title {
		margin: 0;
		color: var(--color-gold);
		font-family: var(--font-display);
		font-size: 0.86rem;
		font-weight: 800;
		letter-spacing: 0.04em;
		line-height: 1.25;
	}

	.nav__features-card-desc {
		margin: 0;
		color: rgba(255, 255, 255, 0.68);
		font-size: 0.76rem;
		line-height: 1.45;
		display: -webkit-box;
		-webkit-box-orient: vertical;
		-webkit-line-clamp: 3;
		overflow: hidden;
	}

	.nav__features-card-current {
		display: inline-flex;
		align-items: center;
		width: fit-content;
		padding: 0.16rem 0.45rem;
		border: 1px solid rgba(197, 138, 69, 0.45);
		color: var(--color-gold-hover);
		font-size: 0.62rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.nav__shop {
		position: relative;
		overflow: hidden;
		isolation: isolate;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		gap: 0.45rem;
		min-height: 3.5rem;
		padding: 0 1.2rem;
		border: 1px solid rgba(255, 228, 170, 0.55);
		border-bottom: 1px solid rgba(255, 228, 170, 0.55);
		background: linear-gradient(135deg, #e8b066 0%, #c58a45 46%, #a86f2f 100%);
		color: #140c04;
		font-size: 0.84rem;
		font-weight: 800;
		letter-spacing: 0.04em;
		text-transform: none;
		text-decoration: none;
		box-shadow:
			0 1px 0 rgba(255, 255, 255, 0.32) inset,
			0 0 0 1px rgba(197, 138, 69, 0.16),
			0 0 20px rgba(197, 138, 69, 0.3),
			0 8px 20px rgba(0, 0, 0, 0.24);
		transition:
			transform 0.2s ease,
			filter 0.2s ease,
			background 0.2s ease,
			border-color 0.2s ease,
			box-shadow 0.2s ease,
			color 0.2s ease;
	}

	.nav__shop::before {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(255, 255, 255, 0.2) 0%, transparent 48%);
		pointer-events: none;
	}

	.nav__shop::after {
		content: '';
		position: absolute;
		top: -10%;
		left: 0;
		width: 42%;
		height: 120%;
		background: linear-gradient(
			105deg,
			transparent 0%,
			rgba(255, 255, 255, 0.12) 42%,
			rgba(255, 255, 255, 0.28) 50%,
			rgba(255, 255, 255, 0.12) 58%,
			transparent 100%
		);
		transform: translateX(-170%) skewX(-18deg);
		pointer-events: none;
	}

	.nav__shop svg {
		position: relative;
		z-index: 1;
	}

	.nav__shop > :not(svg) {
		position: relative;
		z-index: 1;
	}

	.nav__shop-wrap {
		position: relative;
		align-self: stretch;
		margin-left: 0.35rem;
	}

	.nav__shop-trigger {
		height: 100%;
	}

	.nav__shop-wrap.is-open .nav__shop-trigger,
	.nav__shop-wrap:focus-within .nav__shop-trigger,
	.nav__shop-trigger:hover {
		color: #140c04;
		border-color: rgba(255, 236, 190, 0.75);
		background: linear-gradient(135deg, #f0bc72 0%, #d39a50 46%, #b47a3c 100%);
		box-shadow:
			0 1px 0 rgba(255, 255, 255, 0.38) inset,
			0 0 0 1px rgba(255, 228, 170, 0.35),
			0 0 26px rgba(197, 138, 69, 0.42),
			0 10px 24px rgba(0, 0, 0, 0.28);
		transform: translateY(-1px);
	}

	@media (hover: hover) and (pointer: fine) {
		.nav__shop-trigger:hover::after,
		.nav__shop-wrap.is-open .nav__shop-trigger::after {
			animation: nav-shop-shine 2.2s ease-in-out infinite;
		}
	}

	@keyframes nav-shop-shine {
		0% {
			transform: translateX(-170%) skewX(-18deg);
		}
		100% {
			transform: translateX(320%) skewX(-18deg);
		}
	}

	.nav__shop-wrap.is-open .nav__shop-trigger {
		border-bottom-color: transparent;
	}

	.nav__shop-caret {
		opacity: 0.85;
	}

	.nav__shop-mega {
		position: absolute;
		top: 100%;
		right: 0;
		z-index: 140;
		width: min(40rem, calc(100vw - 2 * var(--page-gutter)));
		padding: 1rem;
		border: 1px solid rgba(197, 138, 69, 0.55);
		border-top: 0;
		background: rgba(18, 16, 14, 0.98);
		box-shadow: 0 24px 56px rgba(0, 0, 0, 0.55);
		opacity: 0;
		visibility: hidden;
		pointer-events: none;
		transform: translateY(-4px);
		transition:
			opacity 0.2s ease,
			transform 0.2s ease,
			visibility 0.2s ease;
	}

	.nav__shop-wrap.is-open .nav__shop-mega,
	.nav__shop-wrap:focus-within .nav__shop-mega {
		opacity: 1;
		visibility: visible;
		pointer-events: auto;
		transform: translateY(0);
	}

	.nav__shop-mega-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.75rem;
	}

	.nav__shop-card {
		display: flex;
		flex-direction: column;
		overflow: hidden;
		border: 1px solid rgba(255, 255, 255, 0.08);
		background: rgba(0, 0, 0, 0.35);
		color: inherit;
		text-decoration: none;
		transition:
			border-color 0.2s ease,
			transform 0.2s ease,
			background 0.2s ease;
	}

	.nav__shop-card:hover {
		border-color: rgba(197, 138, 69, 0.45);
		background: rgba(197, 138, 69, 0.04);
		transform: translateY(-1px);
	}

	.nav__shop-card-media {
		position: relative;
		aspect-ratio: 16 / 10;
		overflow: hidden;
		background: rgba(255, 255, 255, 0.04);
	}

	.nav__shop-card-media img {
		display: block;
		width: 100%;
		height: 100%;
		object-fit: cover;
	}

	.nav__shop-card-icon {
		position: absolute;
		top: 0.55rem;
		right: 0.55rem;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 1.65rem;
		height: 1.65rem;
		border: 1px solid rgba(255, 255, 255, 0.14);
		border-radius: 0.15rem;
		background: rgba(0, 0, 0, 0.55);
		color: rgba(255, 255, 255, 0.82);
	}

	.nav__shop-card-icon svg {
		width: 0.85rem;
		height: 0.85rem;
	}

	.nav__shop-card-body {
		padding: 0.85rem 0.95rem 1rem;
	}

	.nav__shop-card-title {
		margin: 0;
		color: var(--color-gold);
		font-family: var(--font-display);
		font-size: 0.92rem;
		font-weight: 800;
		letter-spacing: 0.04em;
		line-height: 1.25;
	}

	.nav__shop-card-desc {
		margin: 0.45rem 0 0;
		color: rgba(255, 255, 255, 0.68);
		font-size: 0.8rem;
		line-height: 1.5;
	}

	.nav__menu-link--shop-mobile {
		display: none;
	}

	.nav__shop svg {
		width: 0.85rem;
		height: 0.85rem;
	}

	.nav__shop--mobile {
		display: none;
		width: 100%;
		margin-top: 0.5rem;
	}

	.nav__toggle {
		display: none;
		place-items: center;
		width: 2.35rem;
		height: 2.35rem;
		border: 1px solid rgba(255, 255, 255, 0.12);
		border-radius: 0.25rem;
		background: rgba(0, 0, 0, 0.35);
		color: #fff;
		cursor: pointer;
	}

	.nav__toggle-icon,
	.nav__toggle-icon::before,
	.nav__toggle-icon::after {
		display: block;
		width: 16px;
		height: 2px;
		border-radius: 2px;
		background: currentColor;
		transition:
			transform 0.25s ease,
			opacity 0.2s ease;
	}

	.nav__toggle-icon {
		position: relative;
	}

	.nav__toggle-icon::before,
	.nav__toggle-icon::after {
		content: '';
		position: absolute;
		left: 0;
	}

	.nav__toggle-icon::before {
		top: -5px;
	}

	.nav__toggle-icon::after {
		top: 5px;
	}

	.nav__toggle[aria-expanded='true'] .nav__toggle-icon {
		background: transparent;
	}

	.nav__toggle[aria-expanded='true'] .nav__toggle-icon::before {
		top: 0;
		transform: rotate(45deg);
	}

	.nav__toggle[aria-expanded='true'] .nav__toggle-icon::after {
		top: 0;
		transform: rotate(-45deg);
	}

	.nav__backdrop {
		display: none;
	}

	@media (max-width: 1080px) {
		.nav__menu-link,
		.nav__menu-link--dropdown {
			padding-inline: 0.5rem;
			font-size: 0.72rem;
		}

		.nav__brand-text {
			font-size: 1.1rem;
		}
	}

	@media (max-width: 960px) {
		.nav__backdrop {
			display: block;
			position: fixed;
			inset: 0;
			z-index: 0;
			border: 0;
			padding: 0;
			margin: 0;
			background: rgba(0, 0, 0, 0.65);
			backdrop-filter: blur(4px);
			cursor: pointer;
			pointer-events: auto;
		}

		.nav__backdrop[hidden] {
			display: none;
		}

		.nav__main {
			position: relative;
			z-index: 2;
			grid-template-columns: 1fr auto;
		}

		.nav__menu {
			position: fixed;
			top: calc(5.1rem + env(safe-area-inset-top, 0px));
			left: var(--page-gutter);
			right: var(--page-gutter);
			z-index: 3;
			display: grid;
			gap: 0;
			max-height: calc(100dvh - 6rem - env(safe-area-inset-top, 0px));
			overflow-y: auto;
			padding: 0.35rem;
			border: 1px solid rgba(255, 255, 255, 0.1);
			background: rgba(12, 10, 8, 0.98);
			box-shadow: 0 20px 48px rgba(0, 0, 0, 0.45);
			opacity: 0;
			visibility: hidden;
			pointer-events: none;
			transform: translateY(-8px);
			transition:
				opacity 0.2s ease,
				transform 0.2s ease,
				visibility 0.2s ease;
		}

		.nav--overlay .nav__menu {
			top: calc(3.5rem + env(safe-area-inset-top, 0px));
			max-height: calc(100dvh - 4.25rem - env(safe-area-inset-top, 0px));
		}

		.nav.is-open .nav__menu {
			opacity: 1;
			visibility: visible;
			pointer-events: auto;
			transform: translateY(0);
		}

		.nav__menu-link,
		.nav__menu-link--dropdown {
			border-left: 0;
			border-bottom: 1px solid rgba(255, 255, 255, 0.06);
			justify-content: space-between;
			min-height: var(--touch-min);
			font-size: 0.82rem;
		}

		.nav__dropdown-panel,
		.nav__utility-panel {
			position: static;
			border: 0;
			box-shadow: none;
			background: rgba(255, 255, 255, 0.03);
			padding: 0;
			opacity: 1;
			visibility: visible;
			pointer-events: auto;
			transform: none;
			display: none;
		}

		.nav__dropdown.is-open .nav__dropdown-panel,
		.nav__utility-dropdown.is-open .nav__utility-panel {
			display: block;
		}

		.nav__features-mega {
			position: static;
			width: 100%;
			padding: 0.65rem 0.35rem 0.75rem;
			border: 0;
			box-shadow: none;
			background: rgba(255, 255, 255, 0.03);
			opacity: 1;
			visibility: visible;
			pointer-events: auto;
			transform: none;
			display: none;
		}

		.nav__features-wrap.is-open .nav__features-mega {
			display: block;
		}

		.nav__features-head {
			text-align: left;
			padding-inline: 0.35rem;
		}

		.nav__features-scroll {
			padding-inline: 0.15rem;
		}

		.nav__dropdown-link,
		.nav__utility-panel-link {
			padding-left: 1.35rem;
		}

		.nav__shop-wrap {
			display: none;
		}

		.nav__shop-mega {
			display: none !important;
		}

		.nav__brand {
			border-right: 0;
			padding-right: 0;
			margin-right: 0;
		}

		.nav__shop--mobile {
			display: inline-flex;
		}

		.nav__menu-link--shop-mobile {
			display: inline-flex;
		}

		.nav__toggle {
			display: grid;
		}
	}

	@media (max-width: 520px) {
		.nav__utility-links {
			gap: 0.85rem;
		}

		.nav__brand-text {
			display: none;
		}
	}
</style>
